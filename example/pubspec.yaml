name: qr_scan_example
description: Demonstrates how to use the qr_scan plugin.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev
version: 0.0.2+2

environment:
  sdk: '>=3.3.1 <4.0.0'
dependencies:
  flutter:
    sdk: flutter

  qr_scan:
    path: ../

dev_dependencies:
  flutter_lints: ^3.0.1

flutter:
  uses-material-design: true